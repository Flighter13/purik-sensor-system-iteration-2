# Receiver layer

The receiver layer will ingest Purik frames from the UNO Q nRF24 interface and later optional transports. It should hand decoded frames upward without embedding sensor-vendor logic.
