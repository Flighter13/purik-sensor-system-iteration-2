# Local API

The UNO Q ground-control backend will expose normalized telemetry and registry state to the touchscreen UI through a local API and/or WebSocket interface.

The API should be capability-driven so the UI can render devices based on registered Purik sensor classes and message schemas rather than hard-coded vendor names.
